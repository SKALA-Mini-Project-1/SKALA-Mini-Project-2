spring.application.name=SKALA-Mini-Project-1
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
server.port=${SERVER_PORT}

# JPA 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SQL 초기화 설정 (Spring Boot 3)
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# 로깅 설정 (초기화 과정 확인용)
logging.level.org.springframework.jdbc.datasource.init=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Redis 설정
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.threads.virtual.enabled=true

# JWT 설정
jwt.secret=your-secret-key-min-32-characters-long-for-hs256-algorithm-security
jwt.expiration=7200000
# 7200000 ms = 2시간

# ==========================================
# 이메일 SMTP 설정
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=fairlineTicket@gmail.com
spring.mail.password=kudkmzqbpchhovjd
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# 이메일 인증 설정
email.verification.code-length=6
email.verification.expiration-minutes=5
email.verification.resend-interval-seconds=60
email.from-address=fairlineTicket@gmail.com
email.from-name=fairlineTicket Team

# TOSS 설정
toss.secret-key=${TEST_SECRET_KEY:}
toss.api-base=${TOSS_API_BASE:https://api.tosspayments.com}
toss.confirm-url=${TOSS_CONFIRM_URL:https://api.tosspayments.com/v1/payments/confirm}

# 로깅 레벨 설정
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=INFO

# 오류 응답에 메시지, 바인딩 오류, 스택 트레이스 포함 설정
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always
